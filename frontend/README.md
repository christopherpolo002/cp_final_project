# WildShapes Frontend

A React-based frontend for the WildShapes animal & shape classifier.

## Setup

1. Install dependencies:
   ```sh
   npm install
   ```
2. Start the development server:
   ```sh
   npm start
   ```

The app will run on http://localhost:3000 and connect to the backend at http://localhost:8000.
